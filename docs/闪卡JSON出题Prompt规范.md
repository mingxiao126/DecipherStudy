# 闪卡生成及 LaTeX 书写规范 Prompt

为了避免在 Decipher Study 闪卡系统中出现 KaTeX 渲染报错（即公式变成红色纯文本形式显示），请你在让 AI 生成出题 JSON 前，**附加上这段 Prompt 约束指令**。

---

## 复制以下 Prompt 喂给 AI：

```markdown
请你扮演一位专业的大学经济学与统计学教授，帮我生成一组用于复习的 Flashcard JSON 数据。

### 数据结构要求
返回的内容必须是一个严格的 JSON 数组（不要 Markdown 代码块包裹，直接输出 JSON 文本即可，或者包裹在 ```json 中），数组的每个元素必须符合以下解构：
[
  {
    "question": "问题题干文字段",
    "answer": {
      "final_answer": "简单直接的最终答案或一句话总结",
      "key_points": [
        "核心要点1",
        "核心要点2"
      ],
      "tested_points": [
        "考察的核心知识点1"
      ],
      "logic_memo": "一句话生动的大白话逻辑记忆锚点"
    },
    "category": "所属大类（如 Inference Statistics）",
    "type": "题目类型（如 概念题 / 对比题 / 计算题）",
    "signals": [
      "题眼关键词1",
      "题眼关键词2"
    ],
    "steps": [
      "Step 1: 怎么想...",
      "Step 2: 怎么做..."
    ],
    "glossary": [
      {
        "term": "专业术语英文",
        "definition": "专业术语定义"
      }
    ],
    "source_reference": "来源标明（如 统计学第六周 P1）"
  }
]

### 🚨 绝对不可违反的 LaTeX 数学公式渲染规范 (非常重要！)
你的输出将被前端框架 KaTeX 渲染，如果你输出了不合法的 LaTeX，会导致页面崩溃或将公式染成红色！请严格遵守以下书写规则：
1. **行内公式**：所有的行内数学变量和公式必须使用 `$...$` 包裹。
2. **禁止孤立的上标/下标**：绝不允许写出 `$z^$` 或 `$t^$` 这种悬空上标。如果表示临界值/阈值，请使用实体字符如 `$z^*$` 包裹或者明确写出 `$z_{\alpha/2}$`。
3. **转义字符的坑**：因为最终输出是 JSON 字符串格式，LaTeX 内部的单反斜杠宏（例如 `\frac` 或 `\mu` 或 `\sqrt`）在 JSON 字符串里只需写原形式（系统底层会自动修复），或者写标准的双反斜杠 `\\frac`。但绝对不能写成畸形的格式（例如避免单独出现的反斜杠被吞掉）。
4. **大于/小于等于**：使用 `\ge` 和 `\le`，而不是 `>=` 和 `<=`。
5. **百分号处理**：单独使用百分号时（如 95%），直接写在文字里，不要包在 `$` 里使用斜杠转义，否则容易引起 JS 解析报错。
6. **常见代数符号的规矩**：
   - 样本均值直接写 `$\bar{x}$`
   - 标准误写 `$SE$` 或 `$SE = \frac{\sigma}{\sqrt{n}}$`
```
